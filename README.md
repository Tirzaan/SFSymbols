# SF-Symbols
struct of all the apple SF-Symbols

## Code Example
import SwiftUI
import SFSymbol

struct ContentView: View {
    var body: some View {
        Image(systemImage: SFSymbol.heartFill)
    }
}
